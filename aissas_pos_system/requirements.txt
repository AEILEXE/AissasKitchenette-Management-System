# Aissas Kitchenette POS System - Requirements

# Core dependencies
# Tkinter is built-in with Python (may require separate package on some Linux distros)
Pillow>=9.0.0
matplotlib>=3.5.0
reportlab>=3.5.0
openpyxl>=3.0.0
# tkcalendar removed — replaced with built-in pure-Tkinter DatePickerDialog

# Optional / packaging
pyinstaller>=5.0.0
pyinstaller-hooks-contrib>=2023.0  # auto-bundles matplotlib mpl-data and other hook-heavy libs
