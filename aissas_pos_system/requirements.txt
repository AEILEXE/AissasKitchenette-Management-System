# Aissas Kitchenette POS System - Requirements
# Python 3.10+ required

# Core UI & image
Pillow>=9.0.0           # product thumbnails, login images, logo rendering
matplotlib>=3.5.0       # sales charts in Reports and Inventory views
reportlab>=3.5.0        # PDF receipt generation
openpyxl>=3.0.0         # Excel export from Reports
# Note: the recommender engine (app/ml/recommender.py) is pure Python — no scikit-learn needed.
# Note: tkinter is built-in with Python (on Linux: sudo apt install python3-tk)

# Packaging (build-time only — not needed at runtime)
pyinstaller>=5.0.0
pyinstaller-hooks-contrib>=2023.0  # auto-bundles matplotlib mpl-data and other hook-heavy libs
